// const check = process.argv;
// console.log('🚀 ~ check:', check);

/* const n1 = +process.argv[2];
const n2 = +process.argv[3];
const opt = process.argv[4];

const myCalulator = (number1, number2, operator) => {
  switch (operator) {
    case '+':
      return number1 + number2;
    case '-':
      return number1 - number2;
    case '*':
      return number1 * number2;
    case '/':
      return number1 / number2;
  }
};

const valueAns = myCalulator(n1, n2, opt);
console.log('🚀 ~ valueAns:', valueAns);

 */
