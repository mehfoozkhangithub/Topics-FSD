# React `learn basic`

- **_JSX_** :- JAVA-SCRIPT and HTML/XML.
  - first we have to import section
  - second we have the function of react components
  - inside the compo we have logic are and our output of the **UI** we put return.

```jsx
import './App.css';

function App() {
  return <h1>hello</h1>;
}

export { App };
```

- what is `<></>` fragment in react?
  - we use this for nested element's to understand react this is whole components.
